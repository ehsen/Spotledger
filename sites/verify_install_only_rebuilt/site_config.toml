[site]
name = "verify_install_only_rebuilt"
namespace = "verify_install_only_rebuilt"

[database]
url = "ws://127.0.0.1:8000"
ns = "verify_install_only_rebuilt"
db = "verify_install_only_rebuilt"
user = "root"
pass = "root"

[cache]
max_documents = 10000
ttl_seconds = 300

[apps]
installed = []
