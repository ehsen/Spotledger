-- Journal Entry Account is a child table (istable = 1).
-- Child tables have no independent pipeline stages.
-- Validation of rows is performed by the parent Journal Entry's functions.

-- This file intentionally contains no function definitions.
-- It exists to satisfy the doctypes directory scanner.
