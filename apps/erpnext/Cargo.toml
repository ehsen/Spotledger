[package]
name        = "erpnext"
description = "SpotledgerCore — ERPNext domain app (Accounts, Buying, Selling, Stock, CRM, Assets, Manufacturing, Projects, Quality Management, Subcontracting)"
version     = "0.1.0"
edition     = "2021"

# Build with: cargo xtask wasm
# Output:     target/wasm32-wasip1/debug/erpnext.wasm  (or release/)
[lib]
crate-type = ["cdylib"]

[dependencies]
# No host deps — schema is pure JSON, logic is SurrealDB events

[workspace]

