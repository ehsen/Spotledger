[package]
name        = "selling"
description = "SpotledgerCore — Selling domain plugin (Sales Order, Sales Invoice, Customer…)"
version     = "0.1.0"
edition     = "2021"

[lib]
crate-type  = ["cdylib"]

# Build with: cargo build --target wasm32-wasip1 -p selling
[dependencies]
# spotledger-pdk = { path = "../../crates/spotledger-pdk" }
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
